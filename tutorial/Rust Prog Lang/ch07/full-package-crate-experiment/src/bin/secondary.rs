// A package can have multiple binary crates by placing files in 
// - the src/bin directory: 
// Each file will be a separate binary crate.

// Binary files are files with their own main function
// Rather than modules that are supportive distinctive purposed code

fn main () {
        println!("Hello, secondary world!");
}